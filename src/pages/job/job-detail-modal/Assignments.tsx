import { Button, HStack, VStack } from "../../../common";

export const Assignments = () => {
  return (
    <VStack>
      <HStack justify="flex-end">
        <Button plusIcon>Add</Button>
      </HStack>
    </VStack>
  );
};
